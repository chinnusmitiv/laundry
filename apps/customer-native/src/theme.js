// Navy ink + lime wordmark, but blue interactive accents (buttons/checkmarks/badges) —
// per explicit product direction, matching customer-web's laundryheap.css skin for
// interactive elements while keeping the lime logo.
export { customerTheme as theme } from '@chaselaundry/shared-native';
